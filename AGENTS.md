# Project Rules

## Common Components
- All shared/common components must be placed in `frontend/common/` folder (at root level)
- Do NOT create common folder inside `components/`
- Use `@/` alias path (e.g., `import DataTable from "@/common/data-table/DataTable"`)
